import                    ./outro_arquivo.well;
import ./outro_arquvo_ainda.well;

public fn main() -> Null{
let a: String = "Isso é uma    string   com muitos espaços";
let a2: String = `Isso é uma string com uma \\ barra inversa`;
let b: String = 'Isso tbm é uma string com espaços \n e uma nova linha';
let b2: String = 'Isso tbm é uma string com uma aspa extra e barra inversa \' ';
let b3: String = "Isso tbm é uma string com uma aspa extra e barra inversa \" ";
let c: String = 'Isso é uma string 
quebrada pelo enter'; //comentário

//isso é um comentário

minha_funcao_em_outro_arquivo(a);

sys.out.warn("sys.out.warn, fn, public, String, //, let, main(), texto pra saber se ele vai ignorar as strings ou não");

let d: String = c + "\nAAAAAAAAAAAAAAAAAAAAAAAAAAA";

let e: MiniInt = {{ sys.out.log("Escopo dentro de escopo, brabo"); }
    let f: Option<MiniInt> = inp.read();

    match f {
        Some(f) => {return f};
        None => {Error("Tipo inválido")};
    };
}

if (e >= 5){
    sys.out.log("Maior ou igual a 5!");
}else{
    sys.out.error("Menor que 5!");
}

}

public type Palavra{
    a: String,
    b: String,
    c: String
}

extend Palavra {
    public fn aMaiusculo(){
        return self.a.toUpperCase();
    } 
}

public let variavel_publica: MiniInt = 127;

public fn testeFuncao(var1:String, var2:String) -> Null {
    sys.out.log("textokk"); //comentário show
    sys.out.log("textokk");
    //Comentário show
    sys.out.log("textokk");
    sys.out.log("textokk");
}

fn funcaoEmUmaLinha() -> Null {sys.out.log("textokk");sys.out.log("OUTRO TEXTO KK");}
